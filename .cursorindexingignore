# =============================================================================
# AstraDraw Cursor Indexing Ignore
# =============================================================================
# Files listed here are excluded from codebase indexing but remain accessible
# to AI features. This improves search accuracy by excluding large content files.
#
# See: https://cursor.com/docs/context/ignore-files

# =============================================================================
# SEPARATE GIT REPOSITORIES
# =============================================================================
# These folders contain separate git repos (not submodules, just cloned here).
# We exclude them from indexing to:
# - Avoid duplicate indexing (they have their own indexes when opened separately)
# - Improve semantic search accuracy in main repo
#
# BUT they remain accessible via @ mentions and direct file reads!
# So AI can still understand cross-repo logic when needed.

frontend/
backend/
room-service/

# =============================================================================
# LARGE DOCUMENTATION FILES
# =============================================================================
# These are useful for reference but shouldn't pollute semantic search results
# when looking for code. They can still be @mentioned directly.

# Cursor plan files (internal planning, not code)
.cursor/plans/

# =============================================================================
# CONFIGURATION FILES
# =============================================================================
# Static configs that rarely need semantic search

deploy/traefik-dynamic.yml
deploy/dex-config.yaml

# =============================================================================
# BACKUP FILES
# =============================================================================

*.bak
*.backup
deploy/docker-compose.override.yml.bak
deploy/docker-compose.override.yml.disabled

